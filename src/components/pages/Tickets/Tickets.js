import React from 'react';
import './Tickets.scss';

class Tickets extends React.Component {
  render() {
    return (
      <div className="Tickets">
        <h2>INSIDE Tickets COMPONENT</h2>
        <button className="btn btn-info">I am a button</button>
      </div>
    );
  }
}

export default Tickets;
